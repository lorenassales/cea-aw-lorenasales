
version: 2

sources:
  - name: sap_adw
    description: Source from Adventure Works sales database.
    schema: sap_adw
    tables:
      
      - name: product
        description: Chart of information about products.        
        columns:
          - name: productid
            description: It is a primary key from product chart.

      - name: productsubcategory
        description: Chart of information about product subcategory.        
        columns:
          - name: productsubcategoryid
            description: It is a primary key from product subcategory chart.

      - name: productmodel
        description: Chart of information about product model.        
        columns:
          - name: productmodelid
            description: It is a primary key from product model chart.

      - name: productcategory
        description: Chart of information about product category.        
        columns:
          - name: productcategoryid
            description: It is a primary key from product category chart.

      - name: address
        description: Chart of information about addresses.        
        columns:
          - name: addressid
            description: It is a primary key from addresses chart.

      - name: stateprovince
        description: Chart of information about state province.        
        columns:
          - name: stateprovinceid
            description: It is a primary key from state province chart.

      - name: countryregion
        description: Chart of information about country.        
        columns:
          - name: countryregioncode
            description: It is a primary key from country chart.

      - name: person
        description: Chart of information about people in general.        
        columns:
          - name: businessentityid
            description: It is a foreign key from BusinessEntityID as a unique key from person chart.

      - name: customer
        description: Chart of information about customer.        
        columns:
          - name: customerid
            description: It is a foreign key from BusinessEntityID as a unique key from customer chart.

      - name: store
        description: Chart of information about stores and sales person assidned to the customer.        
        columns:
          - name: businessentityid
            description: It is a foreign key from BusinessEntityID as a unique key from store chart.

      - name: productvendor
        description: Chart of information about purchased history product cost.

      - name: vendor
        description: Chart of information about verndores.        
        columns:
          - name: businessentityid
            description: It is a foreign key from BusinessEntityID as a unique key from vendor chart.

      - name: purchaseorderdetail
        description: Chart of information about purchase details.        
        columns:
          - name: purchaseorderdetailid
            description: It is a primary key from purchase order details chart.

      - name: purchaseorderheader
        description: Chart of information about purchases.        
        columns:
          - name: purchaseorderid
            description: It is a primary key from purchases chart.

      - name: employee
        description: Chart of information about employees.        
        columns:
          - name: businessentityid
            description: It is a foreign key from BusinessEntityID as a unique key from employees chart.

      - name: salesperson
        description: Chart of information about sales person.        
        columns:
          - name: businessentityid
            description: It is a foreign key from BusinessEntityID as a unique key from sales person chart.

      - name: creditcard
        description: Chart of information about credit card.        
        columns:
          - name: creditcardid
            description: It is a primary key from credit card chart.   

      - name: salesreason
        description: Chart of information about possible sales reasons.
        columns:
          - name: salesreasonid
            description: It is a primary key from sales reason chart.

      - name: salesorderheadersalesreason
        description: Chart of information about the reason to that sale.
        columns:
          - name: salesorderid
            description: It is a foreign key from SalesOrderHeader to make a link with sales reason.

      - name: salesorderdetail
        description: Chart of information about the sales order details.
        columns:
          - name: salesorderdetailid
            description: It is a primary key from sales order details chart.

      - name: salesorderheader
        description: Chart of information about the sales order.
        columns:
          - name: salesorderid
            description: It is a primary key from sales order chart.